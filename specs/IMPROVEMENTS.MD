# Improvements

Nothing outstanding. Everything specified since the build has shipped and moved
to [done](done/README.md), along with the two standing decisions it
settled — phones being out of scope, and how the wire stays compatible across
builds.

New work goes here first: what it is, why it is that and not the obvious
alternative, what it touches, and what done looks like. It moves to
[done](done/README.md) once it is built, with a note at the top
saying where the build diverged from the plan.
